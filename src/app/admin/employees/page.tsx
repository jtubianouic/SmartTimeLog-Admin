import { Users } from "lucide-react";
import { SchemaPending } from "@/components/admin/schema-pending";

export default function EmployeesPage() {
  return <SchemaPending icon={Users} title="Employees" />;
}